name: Schedule Netlify Build
on:
  schedule:
    - cron: "0 8-20/4 * * *"
jobs:
  build:
    name: Call Netlify build hook
    runs-on: ubuntu-latest
    steps:
      - name: Curl request
        run: curl -X POST -d {} $ {{ NETLIFY_BUILDHOOK }}